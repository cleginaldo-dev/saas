version: '3.7'

services:
  pg:
    container_name: pg_saas
    image: bitnami/postgresql:latest
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=saas
      - POSTGRES_PASSWORD=saas
      - POSTGRES_DB=saas